# Never commit exported material or the salt.
*.zip
*.diff
out/
exports/
.deident-private/
salt

# Real session logs must never enter the repo, even as fixtures.
*.jsonl
!test/fixtures/**/*.jsonl

node_modules/
.env
.env.*
*.log
.DS_Store
Thumbs.db

# scan/review/export artifacts, which carry raw paths and workspace names.
review.md
review.html
deident-candidates.txt
deident-entities.json
# The remembered entity dictionary. It lives in ~/.deident-private, which is
# already ignored above; this line is for a copy somebody brings in to look at.
entities.json
deident-triage.txt
deident-triage.json
export-map.txt
# The label the export writes beside its artifacts.
WHAT-TO-SEND.txt
EXPORT-DECISIONS.md
esc.mjs
.impeccable/
